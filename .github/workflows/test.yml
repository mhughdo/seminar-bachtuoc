name: TestDeployReactApp
on:
  push:
    branches:
      - master
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2 # Pull code tu repo ve may ao
      - name: Use Node.js # Cai node
        uses: actions/setup-node@v1
        with:
          node-version: '12.x'
      - run: ls -la
      - run: npm install
      - run: npm run build






